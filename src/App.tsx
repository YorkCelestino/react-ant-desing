import React, { FC } from 'react';
// import { Button } from 'antd';
import './App.css';
import RegistrationForm from './registration_form';

const App: FC = () => (
  <div className="App">
    <RegistrationForm />
  </div>
);

export default App;
